"use client";

import React from "react";

// Theme toggle removed - app is always in dark mode
export default function PageThemeToggle({ showText = true }: { showText?: boolean }) {
  // Always return null - no toggle needed since we're always in dark mode
  return null;
}
